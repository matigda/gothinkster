sudo: false
language: php
php:
  - "7.1"
  - "7.2"

env:
  - DB=sqlite
  - DB=mysql

before-install:
  - composer self-update

install:
  - make build

script:
  - make test

cache:
  directories:
    - $HOME/.composer/cache